# Defaults for icecast initscript
# sourced by /etc/init.d/icecast
# installed at /etc/default/icecast by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Full path to the server configuration file
CONFIGFILE="/etc/icecast/icecast.xml"

# Name or ID of the user and group the daemon should run under
USERID=icecast
GROUPID=icecast

# Edit /etc/icecast/icecast.xml and change at least the passwords.
# Change this to true when done to enable the init.d script
ENABLE=true

